# ARIS Configuration Template
# Copy this file to .env and configure your settings
# API keys are OPTIONAL in .env - prefer storing in system keyring via: aris config set-key

# =============================================================================
# PROJECT PATHS
# =============================================================================

# Project root directory (defaults to current working directory)
# ARIS_PROJECT_ROOT=/path/to/project

# Directory for research documents (defaults to ./research)
# ARIS_RESEARCH_DIR=/path/to/research

# Database file path (defaults to ./.aris/metadata.db)
# ARIS_DATABASE_PATH=/path/to/aris.db

# Cache directory (defaults to ./.aris/cache)
# ARIS_CACHE_DIR=/path/to/cache

# =============================================================================
# LLM CONFIGURATION
# =============================================================================

# Preferred LLM provider (claude_sonnet_4.5, gpt_5, gemini_pro_2.0)
# Default: gemini_pro_2.0 (cost-optimized)
ARIS_PREFERRED_LLM=gemini_pro_2.0

# Fallback LLM provider (used if preferred fails)
ARIS_FALLBACK_LLM=claude_sonnet_4.5

# =============================================================================
# API KEYS (OPTIONAL - PREFER KEYRING)
# =============================================================================
#
# SECURITY WARNING: Storing API keys in .env is NOT recommended for production.
# Use system keyring instead:
#   aris config set-key tavily YOUR_KEY_HERE
#
# API keys in .env are only used if NOT found in keyring (fallback).
# =============================================================================

# Tavily API Key (required for web search)
# Get yours at: https://app.tavily.com
# ARIS_TAVILY_API_KEY=tvly-xxxxxxxxxxxxxxxxxxxxx

# Anthropic API Key (required for Claude validation)
# Get yours at: https://console.anthropic.com
# ARIS_ANTHROPIC_API_KEY=sk-ant-xxxxxxxxxxxxxxxxxxxxx

# OpenAI API Key (required for GPT validation and embeddings)
# Get yours at: https://platform.openai.com
# ARIS_OPENAI_API_KEY=sk-xxxxxxxxxxxxxxxxxxxxx

# Google API Key (optional for Gemini validation)
# Get yours at: https://makersuite.google.com/app/apikey
# ARIS_GOOGLE_API_KEY=AIzaSyxxxxxxxxxxxxxxxxxxxxx

# Context7 API Key (required for technical documentation retrieval)
# Get yours at: https://context7.com
# ARIS_CONTEXT7_API_KEY=ctx7_xxxxxxxxxxxxxxxxxxxxx

# =============================================================================
# BUDGET CONFIGURATION
# =============================================================================

# Default budget for quick research (USD)
ARIS_DEFAULT_BUDGET_QUICK=0.20

# Default budget for standard research (USD)
ARIS_DEFAULT_BUDGET_STANDARD=0.50

# Default budget for deep research (USD)
ARIS_DEFAULT_BUDGET_DEEP=2.00

# Monthly budget limit across all research (USD)
ARIS_MONTHLY_BUDGET_LIMIT=50.00

# =============================================================================
# RESEARCH PARAMETERS
# =============================================================================

# Maximum research hops (depth of investigation)
ARIS_MAX_HOPS=5

# Semantic similarity threshold for document deduplication (0.0-1.0)
# Higher = stricter matching, lower = more aggressive deduplication
ARIS_SEMANTIC_SIMILARITY_THRESHOLD=0.85

# Target confidence score for research completion (0.0-1.0)
ARIS_CONFIDENCE_TARGET=0.70

# Early stop confidence (if reached, stop research early to save costs)
ARIS_EARLY_STOP_CONFIDENCE=0.85

# =============================================================================
# MCP SERVER CONFIGURATION
# =============================================================================

# Tavily endpoint (uses API directly, leave empty)
# ARIS_TAVILY_ENDPOINT=

# Sequential MCP command (local installation)
ARIS_SEQUENTIAL_MCP_PATH=npx

# Serena MCP command (local installation)
ARIS_SERENA_MCP_PATH=npx

# Playwright MCP command (local installation)
ARIS_PLAYWRIGHT_MCP_PATH=npx

# =============================================================================
# PERFORMANCE TUNING
# =============================================================================

# Cache TTL in seconds (default: 3600 = 1 hour)
ARIS_CACHE_TTL_SECONDS=3600

# Maximum parallel search operations
ARIS_MAX_PARALLEL_SEARCHES=5

# Request timeout in seconds
ARIS_REQUEST_TIMEOUT_SECONDS=30

# =============================================================================
# QUALITY THRESHOLDS
# =============================================================================

# Minimum source credibility score (0.0-1.0)
ARIS_MIN_SOURCE_CREDIBILITY=0.5

# Require validation when confidence is below this threshold (0.0-1.0)
ARIS_REQUIRE_VALIDATION_BELOW_CONFIDENCE=0.6

# =============================================================================
# DEVELOPMENT / TESTING
# =============================================================================

# Set to "testing" to use test configuration profile
# ARIS_CONFIG_PROFILE=development

# Enable debug logging (true/false)
# ARIS_DEBUG=false

# =============================================================================
# SETUP INSTRUCTIONS
# =============================================================================
#
# 1. Copy this file:
#    cp .env.example .env
#
# 2. Configure API keys securely (RECOMMENDED):
#    aris config set-key tavily YOUR_TAVILY_KEY
#    aris config set-key anthropic YOUR_ANTHROPIC_KEY
#    aris config set-key openai YOUR_OPENAI_KEY
#    aris config set-key google YOUR_GOOGLE_KEY
#
# 3. Or set keys in .env (NOT RECOMMENDED for production):
#    Edit the ARIS_*_API_KEY variables above
#
# 4. Verify configuration:
#    aris config show
#    aris config validate
#
# 5. Test API keys:
#    aris config test-keys
#
# =============================================================================
# SECURITY BEST PRACTICES
# =============================================================================
#
# ✓ DO: Use system keyring for API keys (aris config set-key)
# ✓ DO: Add .env to .gitignore
# ✓ DO: Use environment-specific .env files (.env.development, .env.production)
# ✓ DO: Rotate API keys regularly
#
# ✗ DON'T: Commit .env files to version control
# ✗ DON'T: Share .env files
# ✗ DON'T: Store production keys in .env files
# ✗ DON'T: Use same API keys across environments
#
# =============================================================================
